<UserControl x:Class="TwitterBusDialog.Views.OKDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="20" VerticalAlignment="Center">
            <Image Source="{Binding ImageSource}" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Message}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </DockPanel>
        <Button Grid.Row="1" Margin="20" Content="OK" Command="{Binding CloseCommand}" Width="75" Height="20"/>
    </Grid>
</UserControl>
